services:
  contagio:
    build: .
    image: contagio:latest
    restart: unless-stopped
    ports:
      - "${CONTAGIO_PORT:-3001}:3001"
    environment:
      NODE_ENV: production
      PORT: 3001
      CORS_ORIGIN: ${CORS_ORIGIN:-*}
